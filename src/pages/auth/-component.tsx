import { AuthWidgetContainer } from '&widgets/auth';

export function Authpage() {
	return (
		<div className="w-full h-screen flex flex-col items-center justify-center">
			<AuthWidgetContainer />
		</div>
	);
}
